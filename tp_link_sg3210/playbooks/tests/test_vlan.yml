---
- name: Test TP-Link VLAN Module
  hosts: localhost
  gather_facts: no
  
  tasks:
    - name: Create VLAN 10
      tp_link_telnet_vlan:
        host: 192.168.0.1
        username: admin
        password: neinnein
        vlan_id: 10
        vlan_name: Management
        state: present
      register: result
      
    - name: Show result
      debug:
        var: result
