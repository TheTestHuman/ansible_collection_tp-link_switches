---
- hosts: localhost
  gather_facts: no
  tasks:
    - name: Create VLAN 10
      tp_link_ssh_vlan_expect:
        host: 192.168.0.1
        username: admin
        password: neinnein
        vlan_id: 10
        vlan_name: Management
