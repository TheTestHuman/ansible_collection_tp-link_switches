---
- name: Configure Core Switches Only
  hosts: core_switches
  gather_facts: no
  connection: local
  
  vars:
    switch_host: "{{ ansible_host }}"
    switch_user: "admin"
    switch_pass: "neinnein"
  
  tasks:
    - name: "Core Switch: {{ inventory_hostname }} - Full Configuration"
      debug:
        msg: "Configuring core switch at {{ switch_location }}"
      
    # ... rest of tasks
